# Upstream Patches

Placeholder — records every approved patch applied under `third_party/`.

## Rules

- Do not modify `third_party/` unless the active task specifically requires an
  approved patch recorded in this file.
- Import tasks (SDK-003 / SDK-004 / SDK-066 / SDK-067) write `third_party/` only
  through the fail-closed import script.
- Each entry must include: component, reason, Issue/PR reference, files touched,
  and whether the patch is upstreamed.

## Patch log

_None yet._
